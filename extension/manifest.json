{
  "manifest_version": 3,
  "name": "WorkPulse Tracker",
  "version": "1.0",
  "description": "Sends the active tab's URL and title to the local WorkPulse agent for accurate website time tracking.",
  "permissions": ["tabs", "activeTab"],
  "host_permissions": ["http://localhost:8001/*"],
  "background": { "service_worker": "background.js" },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"]
    }
  ]
}
