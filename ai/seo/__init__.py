"""MODULE 27 — SEO content-generation logic that combines the generic LLM
provider layer (ai/llm/, module 25) with a page's actual content. Lives
here rather than automation/seo/ because it's model reasoning, not an
external-service call — automation/seo/ is for CMS/Google API clients.
"""
