"""MODULE 27 — Generic image provider layer for the SEO Agentic AI system.

Mirrors ai/llm/ exactly, for the same reason: local/free by default
(FastSD CPU, then Pexels), with paid providers (Stability AI today, more
addable the same way) wired in behind the same interface but inactive
until a key is configured — a config change, never a pipeline rewrite.
"""
